{
  "name": "campfire-fm",
  "private": true,
  "workspaces": [
    "app",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev -w app\" \"npm run dev -w server\"",
    "build": "npm run build -w app",
    "start": "npm run start -w server",
    "typecheck": "echo \"typecheck not configured yet\"",
    "lint": "echo \"lint not configured yet\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
